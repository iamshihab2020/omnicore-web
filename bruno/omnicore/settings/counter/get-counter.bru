meta {
  name: Get Counter
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/settings/counters/{{counterId}}/
  body: none
  auth: {
    type: bearer
    bearer: {{accessToken}}
  }
}

headers {
  X-Tenant-Slug: {{tenantSlug}}
}

docs {
  # Get Counter
  
  This endpoint retrieves details of a specific counter.
  
  ## URL Parameters
  
  - `counterId`: The UUID of the counter to retrieve
  
  ## Response
  
  Returns the counter object with its details.
}
