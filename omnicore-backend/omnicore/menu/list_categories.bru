meta {
  name: List Menu Categories
  type: http
  seq: 1
}

get {
  url: {{base_url}}/menu/categories/
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{tenant_access_token}}
  Content-Type: application/json
}

tests {
  test("Can list menu categories", function() {
    expect(response.status).to.equal(200);
  });
}
