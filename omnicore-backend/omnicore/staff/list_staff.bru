meta {
  name: List Staff
  type: http
  seq: 1
}

get {
  url: {{base_url}}/staff/members/
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{tenant_access_token}}
  Content-Type: application/json
}

tests {
  test("Can list staff members", function() {
    expect(response.status).to.equal(200);
  });
}
