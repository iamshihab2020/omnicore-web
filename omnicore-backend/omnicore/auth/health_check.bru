meta {
  name: API Health Check
  type: http
  seq: 0
}

get {
  url: http://localhost:8000/api/v1/auth/test-auth/
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("API is reachable", function() {
    expect(response.status).to.be.within(200, 500);
  });
}
